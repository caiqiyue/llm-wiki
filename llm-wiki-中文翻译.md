# LLM Wiki

一种使用 LLM 构建个人知识库的模式。

这是一份“想法文件”，它被设计成可以直接复制粘贴给你自己的 LLM Agent（例如 OpenAI Codex、Claude Code、OpenCode / Pi 等）。它的目标是传达这个方案的高层思路，而你的 Agent 会在与你协作的过程中补全具体实现细节。

## 核心思想

大多数人使用 LLM 和文档的方式，看起来都像 RAG：你上传一批文件，LLM 在查询时检索相关片段，然后生成答案。这种方式能工作，但 LLM 每次都要从零开始重新发现知识。假如你问了一个微妙的问题，而这个问题需要综合五份文档，LLM 每次都得重新找到并拼接那些相关片段。知识不会积累。NotebookLM、ChatGPT 的文件上传，以及大多数 RAG 系统，本质上都是这样工作的。

这里的想法不同。它不是在查询时仅仅从原始文档中检索，而是让 LLM **增量式地构建并维护一个持久化的 Wiki**：这是一组结构化、相互链接的 Markdown 文件，位于你与原始资料之间。每当你加入一个新来源时，LLM 并不是简单地为它建立索引、留待以后检索，而是会真正去读它、提取关键信息，并将其整合进已有 Wiki 中：更新实体页面、修订主题摘要、标注新数据与旧结论的冲突、强化或挑战当前逐渐演化出的综合判断。知识只需要被“编译”一次，然后持续保持最新，而不是在每个问题上反复重新推导。

这就是关键差异：**Wiki 是一个持久存在、会不断复利增长的知识产物。** 交叉引用早就已经在那里了，矛盾点也早已被标出来了，综合结论也已经反映了你读过的所有内容。随着你不断加入新的资料、不断提出新的问题，这个 Wiki 会越来越丰富。

你几乎不需要自己写这个 Wiki，或者说只需要极少手动编写，整个 Wiki 都由 LLM 来写和维护。你负责提供资料、进行探索、提出正确的问题。LLM 负责所有繁重的杂活：摘要、交叉引用、归档整理、台账维护，这些事情才是真正让知识库随着时间推移变得有用的关键。就实践而言，我通常是一边开着 LLM Agent，一边开着 Obsidian。LLM 根据我们的对话实时修改 Wiki，而我实时浏览结果：点开链接、查看图谱视图、阅读更新后的页面。Obsidian 是 IDE，LLM 是程序员，Wiki 是代码库。

这个思路可以应用到很多不同场景。例如：

- **个人生活**：跟踪自己的目标、健康、心理状态、自我提升，把日记、文章、播客笔记归档进去，随着时间推移逐步构建出一幅结构化的“自我画像”。
- **研究**：在一个主题上连续深入几周或几个月，阅读论文、文章、报告，逐步构建一个全面的 Wiki，并让其中的核心论点持续演化。
- **读一本书**：每读完一章就归档进去，逐步建立人物、主题、情节线索，以及它们之间的连接。等整本书读完时，你已经拥有一个丰富的伴读 Wiki。可以想想像 [Tolkien Gateway](https://tolkiengateway.net/wiki/Main_Page) 这样的粉丝 Wiki：成千上万个互相链接的页面，覆盖人物、地点、事件、语言，由社区志愿者多年维护而成。你完全可以在个人阅读过程中构建一个类似的东西，让 LLM 来负责所有交叉引用和维护工作。
- **企业 / 团队**：由 LLM 维护的内部 Wiki，输入来源包括 Slack 讨论串、会议转录、项目文档、客户电话记录等。也可以考虑保留人工审核环节。因为没人愿意做维护工作，所以团队 Wiki 常常很快过时；而这里正好由 LLM 来承担维护，因此 Wiki 可以保持最新。
- **竞品分析、尽职调查、旅行规划、课程笔记、兴趣深挖**：任何一种需要随着时间不断积累知识、并且希望这些知识被组织起来而不是四散在各处的场景，都适用。

## 架构

这里有三层：

**原始资料（Raw sources）**：你精心挑选的一组源文档。可以是文章、论文、图片、数据文件等。它们是不可变的：LLM 只读取它们，绝不会修改它们。这一层是你的事实源头（source of truth）。

**Wiki**：一个由 LLM 生成的 Markdown 文件目录。里面可能有摘要页、实体页、概念页、对比页、总览页、综合分析页。LLM 完全拥有这一层：它创建页面，在新资料到来时更新页面，维护交叉引用，并尽量保持整体一致性。你来读它；LLM 来写它。

**Schema（模式 / 规范）**：一份文档（例如给 Claude Code 用的 `CLAUDE.md`，或给 Codex 用的 `AGENTS.md`），告诉 LLM 这个 Wiki 应该如何组织、采用什么约定，以及在摄取资料、回答问题、维护 Wiki 时该遵循哪些工作流。这是最关键的配置文件。它决定了 LLM 是一个纪律良好的 Wiki 维护者，还是一个泛泛而谈的聊天机器人。随着你们逐步摸索出适合你所在领域的工作方式，这份规范也会与你和 LLM 一起共同演化。

## 操作方式

**摄取（Ingest）**。你把一个新来源放进原始资料集合，然后告诉 LLM 去处理它。一个示例流程可能是：LLM 读取该资料，与你讨论关键结论，在 Wiki 中写一页摘要，更新索引，更新 Wiki 中所有相关的实体页和概念页，并在日志里追加一条记录。一个来源可能会影响 10 到 15 个 Wiki 页面。就我个人而言，我更喜欢一次只摄取一个来源，并且全程参与：我会阅读摘要、检查更新，并告诉 LLM 应该强调什么。但你也可以一次批量摄取很多来源，只是监督更少。最终该怎么做，取决于你想形成什么样的工作流；把它写进 schema 里，供未来的会话复用即可。

**查询（Query）**。你围绕 Wiki 提问。LLM 搜索相关页面，读取它们，并带着引用进行综合回答。根据问题不同，答案可以有不同形式：一篇 Markdown 页面、一个对比表格、一份幻灯片（Marp）、一张图表（matplotlib）、一个画布。这里最重要的洞见是：**优质答案本身也可以反过来写回 Wiki，成为新的页面。** 你让它做的对比、一段分析、你发现的一条关联线索，这些都是有价值的，不应该消失在聊天记录里。这样一来，你的探索过程也会像摄取新来源一样，在知识库中持续复利增长。

**体检 / 清理（Lint）**。定期让 LLM 对 Wiki 做一次健康检查。检查内容包括：页面之间是否有矛盾、较新的来源是否已经推翻了旧结论、是否存在没有入链的孤立页面、是否有重要概念已经被提到却还没有自己独立的页面、是否缺少交叉引用、是否存在可以通过网络搜索补上的信息空缺。LLM 很擅长提出下一步值得追问的问题，以及值得继续寻找的新来源。这个步骤能让 Wiki 在不断变大时仍然保持健康。

## 索引与日志

有两个特殊文件可以帮助 LLM（也帮助你）在 Wiki 规模增长后依然能顺畅导航。它们各自承担不同职责：

**`index.md`** 是面向内容的。它是整个 Wiki 的目录：列出每个页面的链接、一句话摘要，以及可选的元数据，例如日期、来源数量等。可以按类别组织，比如实体、概念、来源等。LLM 在每次摄取时都会更新它。回答问题时，LLM 会先读索引，找到相关页面，再进一步钻取这些页面。在中等规模下（约 100 个来源、数百个页面），这种方式出奇地有效，而且避免了额外搭建基于向量嵌入的 RAG 基础设施。

**`log.md`** 是按时间顺序排列的。它是一份只追加、不回写的记录，记下发生了什么、发生在什么时候：摄取、查询、Lint 检查等等。一个实用技巧是：如果每条记录都以统一前缀开头（例如 `## [2026-04-02] ingest | Article Title`），那么这个日志就可以被简单的 Unix 工具解析，例如 `grep "^## \\[" log.md | tail -5` 就能取出最后 5 条记录。日志能够为你展示 Wiki 的演化时间线，也帮助 LLM 理解最近已经做过哪些事情。

## 可选项：CLI 工具

到某个阶段，你可能会想做一些小工具，让 LLM 更高效地操作这个 Wiki。最显而易见的是给 Wiki 页面做一个搜索引擎。在规模较小时，索引文件足够；但当 Wiki 继续增长时，你会想要一个真正的搜索能力。[qmd](https://github.com/tobi/qmd) 是一个不错的选择：它是一个面向 Markdown 文件的本地搜索引擎，支持混合 BM25 / 向量搜索以及 LLM 重排序，而且全都在本机运行。它既提供 CLI（因此 LLM 可以通过 shell 调用），也提供 MCP 服务器（因此 LLM 可以把它当作原生工具来用）。你也完全可以自己做一个更简单的版本，等需求出现时再让 LLM 帮你“vibe code”一个朴素搜索脚本即可。

## 实用技巧

- **Obsidian Web Clipper** 是一个浏览器扩展，可以把网页文章转换成 Markdown。它非常适合快速把资料放进你的原始资料集合。
- **把图片下载到本地。** 在 Obsidian 设置里，进入 “Files and links”，把 “Attachment folder path” 设为一个固定目录（例如 `raw/assets/`）。然后在 “Hotkeys” 里搜索 “Download”，找到 “Download attachments for current file” 并绑定快捷键（例如 `Ctrl+Shift+D`）。这样你裁剪完一篇文章后，按一下快捷键，所有图片就会被下载到本地磁盘。这一步是可选的，但很有用，因为这样 LLM 就能直接查看和引用图片，而不是依赖可能失效的 URL。需要注意的是，LLM 并不能在一个步骤里原生读取“带内嵌图片的 Markdown”。一种变通办法是：先让 LLM 读取正文文本，再分别查看部分或全部被引用的图片，以补充额外上下文。虽然有点笨拙，但已经足够实用。
- **Obsidian 的图谱视图（graph view）** 是理解整个 Wiki 结构的最佳方式：哪些页面互相连接，哪些是枢纽，哪些是孤儿页面，一目了然。
- **Marp** 是一种基于 Markdown 的幻灯片格式。Obsidian 有对应插件。它非常适合直接从 Wiki 内容生成演示文稿。
- **Dataview** 是 Obsidian 的一个插件，可以对页面 frontmatter 进行查询。如果你的 LLM 会给 Wiki 页面添加 YAML frontmatter（例如标签、日期、来源计数），Dataview 就能生成动态表格和列表。
- 整个 Wiki 本质上只是一个由 Markdown 文件组成的 Git 仓库。于是你天然就获得了版本历史、分支能力和协作能力。

## 为什么这个方案有效

维护知识库最烦人的部分，不是阅读，也不是思考，而是“记账式维护”。更新交叉引用、保持摘要最新、标记新数据何时推翻旧结论、维护几十个页面之间的一致性。人类之所以放弃 Wiki，往往不是因为它没有价值，而是因为维护成本增长得比价值更快。LLM 不会觉得无聊，不会忘记补一个交叉引用，而且能在一次操作中改 15 个文件。因此，Wiki 能保持被维护的状态，是因为维护成本几乎被压到了接近零。

人的工作是：筛选来源、引导分析、提出好问题，并思考这些材料真正意味着什么。LLM 的工作，则是除此之外的其他一切。

这个想法在精神上与 Vannevar Bush 在 1945 年提出的 Memex 很相近：一个私人的、经过筛选整理的知识存储系统，文档之间通过“联想轨迹”相互连接。Bush 的愿景其实比今天的 Web 更接近这里的形态：它是私人的、主动整理的，而且文档之间的连接和文档本身一样有价值。他当年没有解决的问题，是“谁来负责维护”。而现在，LLM 可以承担这部分工作。

## 注

这份文档是有意保持抽象的。它描述的是一种模式，而不是某个具体实现。目录结构怎么设计、schema 约定如何制定、页面格式如何选择、工具链如何搭建，这些都取决于你的领域、你的偏好，以及你选择的 LLM。上文提到的所有东西都是可选的、模块化的：有用的就拿来用，没用的就忽略。举几个例子：如果你的来源全是纯文本，那么你根本不需要图像处理；如果你的 Wiki 规模很小，索引文件就足够，不需要搜索引擎；如果你不关心幻灯片输出，就完全可以只保留 Markdown 页面；如果你想要的是另一套输出格式，也没有问题。正确的使用方式是：把这份文档分享给你的 LLM Agent，然后和它一起把这个模式实例化成适合你自己的版本。这份文档唯一的职责，就是传达这个模式。至于其余部分，你的 LLM 可以和你一起推导出来。
